{
  "name": "ts-project-test",
  "version": "1.0.0",
  "scripts": {
    "tsc": "rm -rf build && tsc",
    "build": "rm -rf build && tsc -p tsconfig.build.json && cp packages/first/package.json build/packages/first/src/ && cp packages/second/package.json build/packages/second/src/",
    "build2": "rm -rf build && tsc -b"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/node20": "^20.1.0",
    "@types/jest": "^29.5.3",
    "tsc-alias": "^1.8.7",
    "typescript": "~5.0.4"
  }
}
