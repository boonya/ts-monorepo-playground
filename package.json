{
  "name": "ts-monorepo-playground",
  "author": "Serhii <boonya> Buinytskyi",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "~20.3.1",
    "npm": "~9.6.7"
  },
  "workspaces": [
    "packages/*",
    "tests"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "echo 'Not implemented yet ;)'",
    "postbuild": "scripts/postbuild.ts",
    "prebuild": "rm -rf build/ packages/**/build/ packages/**/*.tsbuildinfo",
    "prerelease": "lerna version --no-private --no-push",
    "release": "lerna publish --contents build",
    "test": "jest",
    "typecheck": "tsc -b tsconfig.json",
    "verify": "npm run lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.0",
    "glob": "^10.3.3",
    "lerna": "^7.1.4",
    "ts-node": "^10.9.1",
    "typescript": "~5.0.4",
    "yargs": "^17.7.2"
  }
}
