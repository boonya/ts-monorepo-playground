name: "Label PR"

on:
  workflow_dispatch: # 👈 For debugging purpose
  workflow_call:
  # pull_request:
  #   types:
  #     - opened
  #     - synchronize

permissions:
  contents: read
  pull-requests: write

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v4
