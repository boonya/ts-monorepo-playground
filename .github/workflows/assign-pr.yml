name: Assign PR

on:
  pull_request:
    types:
      - opened

permissions:
  pull-requests: write

jobs:
  assign:
    runs-on: ubuntu-latest
    name: Assign PR to autor
    steps:
      - uses: toshimaru/auto-author-assign@v1.6.2
