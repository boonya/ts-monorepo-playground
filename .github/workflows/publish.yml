name: Publish 🚚

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      preid:
        required: true
        default: alpha
        type: choice
        options:
          - alpha
          - beta
      preDistTag:
        required: true
        default: test
        type: choice
        options:
          - test
          - dev

jobs:
  test:
    name: "Test 👮🏽‍♂️"
    uses: ./.github/workflows/test.yml
    secrets: inherit

  publish:
    needs: test
    runs-on: ubuntu-latest
    name: Publishing 🚀
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node-version-file: ".nvmrc"
      - run: npm set '//npm.pkg.github.com/:_authToken=${{ secrets.GHPR_READ_TOKEN }}'
      - run: npm ci
      - run: npm run build
      - if: github.event_name == 'push'
        name: "Release"
        run: |
          lerna publish from-package --yes \
            --dist-tag alpha
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GHPR_RW_TOKEN }}
      - if: github.event_name == 'workflow_dispatch'
        name: "Pre-Release"
        run: |
          lerna publish --yes \
            --canary \
            --preid ${{ inputs.preid }} \
            --dist-tag ${{ inputs.preDistTag }} \
            --pre-dist-tag ${{ inputs.preDistTag }} \
            --force-publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GHPR_RW_TOKEN }}
